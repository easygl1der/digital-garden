---
permalink: 404.html
---
{%- set basePath = meta.basePath -%}
<!DOCTYPE html>
<html lang="{{ meta.mainLanguage }}">
    <head>
        <title>Nothing here</title>
        <link href="{{ basePath }}styles/digital-garden-base.css" rel="stylesheet">
        {%-if meta.themeStyle%}
            <link href="{{ basePath }}styles/obsidian-base.css" rel="stylesheet">
    	    <link href="{{ basePath }}{{meta.themeStyle}}" rel="stylesheet">
        {% else %}
            <link href="{{ basePath }}styles/style.css" rel="stylesheet">
        {%endif%}

        <link href="{{ basePath }}styles/custom-style.css" rel="stylesheet">

    </head>
    <body class="theme-{{meta.baseTheme}} markdown-preview-view">
        <div class="content centered">

            {%-if not meta.themeStyle%}
                <div class="font-bg"> &#x1F60E; </div>
            {%endif%}
            <h1>There is nothing here</h1>
            <p>If you got here from a link, this note is probably not made public</p>
            <a href="{{ basePath }}">Go back home</a>
        </div>
    </body>
</html>

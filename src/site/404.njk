---
permalink: 404.html
---
{%- set basePath = meta.basePath -%}
{%- set assetPath = meta.assetPath -%}
<!DOCTYPE html>
<html lang="{{ meta.mainLanguage }}">
    <head>
        <title>Nothing here</title>
        <link href="{{ assetPath }}styles/digital-garden-base.css" rel="stylesheet">
        {%-if meta.themeStyle%}
            <link href="{{ assetPath }}styles/obsidian-base.css" rel="stylesheet">
    	    <link href="{{ assetPath }}{{meta.themeStyle}}" rel="stylesheet">
        {% else %}
            <link href="{{ assetPath }}styles/style.css" rel="stylesheet">
        {%endif%}

        <link href="{{ assetPath }}styles/custom-style.css" rel="stylesheet">

    </head>
    <body class="theme-{{meta.baseTheme}} markdown-preview-view">
        <div class="content centered">

            {%-if not meta.themeStyle%}
                <div class="font-bg"> &#x1F60E; </div>
            {%endif%}
            <h1>There is nothing here</h1>
            <p>If you got here from a link, this note is probably not made public</p>
            <a href="{{ basePath }}">Go back home</a>
        </div>
    </body>
</html>
